import type { VehicleCategory } from "../data/inventory";

export function vehicleCategoryAlt(category: VehicleCategory): string {
  return `${category} — Temptation Motorsports powersports financing`;
}
